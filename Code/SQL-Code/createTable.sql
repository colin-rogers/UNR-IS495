CREATE TABLE author (
	AuthorID int,
	FirstName varchar(255),
	LastName varchar(255)
);

CREATE TABLE publisher (
	FirstName varchar(255),
	LastName varchar(255),
	YearPublished varchar(4)
);
